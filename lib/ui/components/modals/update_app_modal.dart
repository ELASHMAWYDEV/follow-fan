import 'package:flutter/material.dart';

class UpdateAppModal extends StatelessWidget {
  const UpdateAppModal({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}